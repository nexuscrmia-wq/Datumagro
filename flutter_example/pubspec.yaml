name: datumagro_flutter_example
description: Example Flutter service for DatumAgro API
version: 0.0.1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
